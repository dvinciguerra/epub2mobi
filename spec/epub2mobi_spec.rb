# frozen_string_literal: true

RSpec.describe Epub2mobi do
  it "has a version number" do
    expect(Epub2mobi::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
